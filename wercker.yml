box: golang

build:
  steps:
    - setup-go-workspace
    - golint
    - script:
        name: go build
        code: go build -a -v ./...
    - script:
        name: go test
        code: go test -v ./...
